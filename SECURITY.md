# Security

Do not open public issues containing credentials, private endpoints, customer
files, or confidential documents.

Local secrets belong in:

```text
config/office-skills.local.env
```

This file is ignored by Git. If you believe a secret was committed, rotate the
credential immediately and open a private maintainer contact path before adding
more public details.
